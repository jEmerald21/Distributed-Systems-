Development on OSX [tested on BigSur Macbook 1.1 GHz Dual-Core Intel Core M]:
=============================================================================

// prepwork using homebrew as packagae manager
-----------------------------------------------
brew install protobuf
brew install --cask bloomrpc

// compilation commands are in compile_all.sh
-----------------------------------------------
// may need "chmod +x ./compile_all.sh" to be execute-able
./compile_all.sh